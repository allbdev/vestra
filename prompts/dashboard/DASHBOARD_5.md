# DASHBOARD 4

I want to be able to see the transaction detail in app/components/dashboard/PeriodTransactionsView.tsx

When clicking on the transaction name should open the edit modal (app/components/transactions/TransactionFormModal.tsx)

Lets make sure that the user knows the text is clicable

It should be a text button (unstyled), when hovering it the cursor should change to pointer and the text should be underlined