# MATERIAL UI

I want to replace raw components with components from Material UI lib

I've already installed it into the project, but we need to replace the existing components on app/components/ui with material ui components

At first I want to replace input (CodeInput should reuse input layout) and button components

I also want to add a select component

Search the code base to see if we have any component created atraight in the page instead of using a component from UI components and replace it